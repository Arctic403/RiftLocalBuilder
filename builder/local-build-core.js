export const RIFT_LOCAL_BUILD_VERSION='H2.10';
export const RIFT_LOCAL_BUILD_JOB_FORMAT='riftcity-local-build-job';
export const RIFT_LOCAL_BUILD_RESULT_FORMAT='riftcity-local-build-result';
const now=()=>Date.now(),clone=v=>v==null?v:JSON.parse(JSON.stringify(v));
export async function sha256Json(value){const b=new TextEncoder().encode(JSON.stringify(value));const d=await crypto.subtle.digest('SHA-256',b);return[...new Uint8Array(d)].map(x=>x.toString(16).padStart(2,'0')).join('')}
export function validateLocalBuildJob(job){if(!job||typeof job!=='object'||Array.isArray(job))throw new Error('Local build job must be an object.');if(job.format!==RIFT_LOCAL_BUILD_JOB_FORMAT)throw new Error(`Job format must be '${RIFT_LOCAL_BUILD_JOB_FORMAT}'.`);if(Number(job.version)!==1)throw new Error('Local build job version must be 1.');const jobId=String(job.job_id||'').trim(),targetPath=String(job.target?.path||'').trim();if(!jobId||jobId.length>180)throw new Error('job_id is required and must be 180 characters or fewer.');if(!targetPath)throw new Error('job.target.path is required.');return{jobId,targetPath}}
export async function runLocalBuildJob(job,program,runtime){const{jobId,targetPath}=validateLocalBuildJob(job);if(!program||typeof program!=='object'||Array.isArray(program))throw new Error('Candidate BuildingProgram must be a JSON object.');const startedAt=now(),candidateSha=await sha256Json(program),m=runtime.module;try{const compiled=m.compileRiftBuildingProgram(program,{strict:true});const documentSha=await sha256Json(compiled.document);const result={format:RIFT_LOCAL_BUILD_RESULT_FORMAT,version:1,local_builder_version:RIFT_LOCAL_BUILD_VERSION,building_pipeline_version:m.RIFT_BUILDING_PIPELINE_VERSION,job_id:jobId,ok:compiled.report.ok===true,target:{path:targetPath,branch:String(job.target?.branch||runtime.source.branch)},runtime_source:runtime.source,candidate_sha256:candidateSha,compiled_document_sha256:documentSha,source_program_id:compiled.report.sourceProgramId,source_fingerprint:compiled.report.sourceFingerprint,plan_fingerprint:compiled.plan?.fingerprint||null,affected_stages:m.getRiftBuildingAffectedStages(job.changed_paths||[]),completed_at:now(),duration_ms:Math.max(0,now()-startedAt),stats:clone(compiled.report.stats),pipeline:clone(compiled.pipeline),diagnostics:clone(compiled.report.diagnostics),repair_tasks:clone(compiled.report.repairTasks)};return{publicResult:result,artifact:{job:clone(job),program:clone(program),document:clone(compiled.document),plan:clone(compiled.plan),pipeline:clone(compiled.pipeline),report:clone(compiled.report),semantics:clone(compiled.semantics)}}}catch(error){const pipeline=clone(error?.pipeline||error?.report?.pipeline||null),diagnostics=clone(error?.diagnostics||error?.report?.diagnostics||[]);return{publicResult:{format:RIFT_LOCAL_BUILD_RESULT_FORMAT,version:1,local_builder_version:RIFT_LOCAL_BUILD_VERSION,building_pipeline_version:m.RIFT_BUILDING_PIPELINE_VERSION,job_id:jobId,ok:false,target:{path:targetPath,branch:String(job.target?.branch||runtime.source.branch)},runtime_source:runtime.source,candidate_sha256:candidateSha,completed_at:now(),duration_ms:Math.max(0,now()-startedAt),error:String(error?.message||error),failed_stage:String(error?.stage||'')||null,plan_fingerprint:error?.plan?.fingerprint||pipeline?.planFingerprint||null,affected_stages:m.getRiftBuildingAffectedStages(job.changed_paths||[]),pipeline,stats:error?.report?.stats||null,diagnostics,repair_tasks:clone(error?.report?.repairTasks||[])},artifact:{job:clone(job),program:clone(program),error:String(error?.message||error)}}}}
